import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.swiper',
  appName: 'cs-swiper',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
